#include <bits/stdc++.h>
using namespace std;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using P = pair<int,int>;

int main() {
    string P; cin >> P;
    int L; cin >> L;
    int n=P.size();

    if(n>=L) cout << "Yes" << endl;
    else cout << "No" << endl;
}
