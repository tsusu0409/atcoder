#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using P = pair<int,int>;

bool check_prime(int N, vector<int> prime) {
    for(auto i: prime)    
}

int main() {
    int Q; cin >> Q;
    int N = 100000;
    vector<bool> check(N);
    rep(i, N){
        
    }

    // 素数判定どうすればいいかわからないそぽ～～～
}