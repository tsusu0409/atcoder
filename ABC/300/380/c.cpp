#include <bits/stdc++.h>
using namespace std;
#include <atcoder/all>
using namespace atcoder;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
using ll = long long;
using P = pair<int,int>;

int main() {
    int N, K; cin >> N >> K;
    string S; cin >> S;
    
    int count = 0;
    int now = 2;
    vector<string> p = {};
    rep(i, N){
        //絶対圧縮じゃんこれ
    }
}
